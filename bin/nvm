nvm() {
    unset -f nvm
    export NVM_DIR="$HOME/.nvm"

    if [ -s "$NVM_DIR/nvm.sh" ]; then
        . "$NVM_DIR/nvm.sh"
        nvm "$@"
    else
        echo "nvm is not installed: $NVM_DIR"
        return 1
    fi
}
