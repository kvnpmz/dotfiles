#!/usr/bin/env bash
args=()
for arg in "$@"; do
    [[ "$arg" =~ ^- ]] && continue  # skip all flags
    args+=("$arg")
done
[ ${#args[@]} -eq 0 ] && exit 0

for file in "${args[@]}"; do
    gio trash -- "$file"
done
