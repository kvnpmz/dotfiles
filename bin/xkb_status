#!/bin/sh

case "$(cat "$HOME/.keyboard_layout" 2>/dev/null)" in
    us) printf '  US\n' ;;
    en) printf '  EN\n' ;;
    gr) printf '  ΕΛ\n' ;;
    il) printf '  עב\n' ;;
    *)  printf '  ??\n' ;;
esac
